using Microsoft.EntityFrameworkCore;
using UnitTest.Common;

namespace Tiny.Infrastructure.UnitTests;

public class GLAccountRepositoryTest
{
    //[Fact]
    //public async void AddAsync()
    //{
    //    var repository = GLAccountRepositoryMock.Get().Object;

    //    var glAccountCount = (await repository.GetAllAsync(default)).Count;

    //    var newGLAccount = GLAccountStore.CreateGLAccountWithId(12, "1010", "계정코드", 1, 1);
    //    await repository.AddAsync(newGLAccount, default);

    //    var expectedGLAccountCount = glAccountCount + 1;

    //    Assert.Equal(glAccountCount, expectedGLAccountCount);
    //}
}
